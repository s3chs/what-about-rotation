import './RotationDefinition.scss';

export default function RotationDefinition() {
    return (
        <div className="rotation-definition-section">
            <div className="rotation-definition-text-container">
                <p>sneaker rotation:</p>
                <p>The act of owning multiple sneakers, allowing one to wear different sneakers everyday, hence creating a
                    rotation</p>
            </div>
        </div>
    );
}
